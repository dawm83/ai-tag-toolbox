# V1.4.320 · ComfyUI 连接与仅 Tag 模式

本次在独立分支 `codex/comfy-tags-mode` 完成，基于桌面稳定版本 V1.4.316（b8ef70c）。没有修改、合并或覆盖另一个 AI 正在处理的收藏／统一标签库分支。

## 修改内容

- ComfyUI 使用 Node 原生 HTTP/HTTPS 请求；保留具体连接错误，单次检测只请求一次服务信息。图片上传、下载和任务提交共用此入口。
- 默认只生成 Tag。首次连接检测完成前不会绘图；确认断线后关闭绘图，重新连接不会自动开启。
- “启用绘图”和连接状态分开显示，“自动执行”改为“自动迭代”。关闭绘图时隐藏迭代设置并保留数值。
- 仅 Tag 模式继续支持角色与参考图，生成完成后直接交付，不检查 ComfyUI 工作流。Tag 可逐个或整组复制，并随对话保存。
- 绘图失败或等待补充信息时，已经生成的 Tag 仍然可见。
- 查询结果时断线会保留已提交的任务编号，点击“恢复原任务”只查询原任务，重启后也能恢复。请求结果未知时不自动重复提交。

## 验证与范围

- `npm run check` 包含源码、架构、单元测试及既有快速回归检查。
- 使用本地模拟服务验证无 CORS 响应头下的连接、上传、下载、提交和取消。
- 使用独立临时目录中的 Electron 31.7.7 验证了原生请求、multipart 图片上传、图片下载和任务提交；该程序没有载入本项目的收藏、图库或对话数据。
- 这次 Electron 验证中，旧的默认 fetch 对模拟服务也能成功。因此不能把原来的所有连接失败断定为浏览器 CORS；真实绘世实例未添加参数时的兼容性仍需人工验收。
- 没有调用真实 AI 或向用户 ComfyUI 提交绘图任务。

## 建议人工验收

1. 不启动 ComfyUI，生成一组 Tag，检查显示、复制和重新打开对话。
2. 用绘世正常启动 ComfyUI，使用正确地址检测连接；验证不添加 CORS 参数时的实际连接情况。
3. 开启绘图后分别测试“只要 Tag”、单轮绘图和自动迭代。
4. 查询图片结果时断开连接，再连接并开启绘图，点击“恢复原任务”，确认 ComfyUI 队列未重复增加任务。

本版本没有推送或发布到 GitHub。收藏分支仍在原工作区继续开发，后续合并应仅移入本分支的 ComfyUI／AI 相关提交。
