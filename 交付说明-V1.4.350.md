# V1.4.350 · 修复续绘候选重复展示

本版修复了续绘任务的显示问题。生成任务内部会保留完整候选历史用于主 AI 判断，但续绘产生的新助手消息只携带本轮新增候选，旧图片继续保留在之前的消息中，不再看起来像被重新生成。

改动包括：

- 主 AI 续改和候选卡“按这张图继续优化”都按续绘开始前的候选 ID 计算增量快照。
- 增量快照同时过滤候选、图片 ID、资源和轮次，避免旧图片在新回复里重复出现。
- 新增两条回归测试，覆盖主 AI 和手动续绘路径。

检查：`npm run check`，717 项通过，0 失败；`git diff --check` 通过。真实模型、ComfyUI 和 Electron 启动仍交由用户人工验收。

本轮未 push、未发布 Release。
