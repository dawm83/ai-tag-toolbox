# V1.4.347 · 灵活图片参考调度

V1.4.347 调整了图片参考和本地识图的调度，让普通文生图、整图复刻和局部属性参考走不同路径。

## 本轮改动

- 没有参考图的普通文生图不会调用 `conversation.listImages`、`conversation.viewImages` 或 `vision.processOne`，也不会传 `sourceImageId`。
- 有多张图片时，主 AI 先判断图片角色：目标复刻图、衣服/姿势等属性参考图，或与当前绘制无关的图片。
- “参考图 A 的衣服，复刻图 B”时，只从 A 提取用户点名的属性 Tag；B 才作为复刻目标进入工作流，A 不会被误传成 ComfyUI 原图。
- 复刻目标没有可靠内置 Tag 时才运行一次本地识图；属性图也只在确实需要时识图，后续修改不重复本地识图。
- 普通文生图的过程提示改为“正在按当前绘图 Tag 生成候选”，避免把文生图误显示成参考图首轮流程。

## 检查与使用

- `npm run check`：713 项通过，0 失败。
- 新增和更新任务路由、主 AI 提示词、普通文生图无识图工具、属性参考调度和过程文案测试。
- 未执行付费 API、真实图片生成、真实 ComfyUI 或慢速 Electron 启动测试；这些仍需用户人工验收。
- 双击 `AI绘画Tag工具箱V1.4.347.exe`。本轮未 push、未发布 Release。
