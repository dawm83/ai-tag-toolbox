# V1.4.342 · 本地精简与性能优化

## 已完成

- 调用监视器只保留请求层级、工具、模型、状态、耗时、HTTP 状态、Token 和错误摘要；详细过程继续在对话中。
- 新增隔离性能探针，启动和数据路径测量不会写入真实用户目录。
- 图片索引恢复不再启动时读取原图；图库列表优先返回元数据和缩略图，预览、识图和下载时才读取原图。
- 图库选择只更新当前卡片；收藏页首屏最多渲染四个栏目，其他栏目按需加载。
- 翻译引用使用英文、中文和别名索引；角色作品统计按版本和筛选条件缓存。
- Tag 选择、取消选择和标记复制使用局部校验；结构编辑仍保留完整校验。
- 流式聊天复用当前消息快照，停止重复保存 activity 副本；默认 Tags 启动不预渲染隐藏聊天。

## 验证

- npm run check：671 项测试通过，0 项失败；回归检查通过。
- 图片、图库、收藏、角色、翻译、调用摘要和聊天定向测试已覆盖。
- 未执行真实模型、ComfyUI 和完整 Electron 冷启动验收。

运行 AI绘画Tag工具箱V1.4.342.exe。本轮未推送、未发布 Release。
