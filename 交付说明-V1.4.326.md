# V1.4.326 · 修复收藏与主页显示同步

修复取消收藏后返回主页仍显示旧星标、旧收藏位置的问题。同一遗漏也会让收藏页修改的原文、显示名称、备注和搜索开关在主页暂时显示旧值。

根因：共享库和查询缓存已更新，但主页隐藏期间没有重绘；返回主页只显示原DOM，未读取最新共享记录。现在从其他页面返回主页时重新读取并渲染当前分类和标签。首屏启动不重复渲染，保留此前启动优化。

数据模型保持一个 tagId 对应一份内容。收藏只增减位置归属，不复制Tag；取消收藏不删除底层Tag。一个Tag有多个收藏位置时，剩余位置仍有效，取消最后一个位置后才变为未收藏。

新增实际页面回归覆盖：取消收藏后星标/位置清除；收藏页修改字段与搜索资格后返回主页同步；多个位置取消与撤销；主页与收藏空白格共用同一个编辑弹窗实例；收藏对话框新增页/栏不增加标签数量；未收藏时移动到分类/子分类、已收藏时移动到收藏页/标签栏；新Tag默认允许搜索且非成人；保存后关闭；旧右侧编辑器未恢复。

验证：`npm run check`，完整源码检查、架构护栏、605项自动测试及既有回归通过。按项目规则未运行Electron慢测，实际窗口交互留待用户反馈。

未访问或修改真实收藏、图库、API或对话数据；未推送或发布GitHub。
