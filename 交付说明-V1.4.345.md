# V1.4.345 · 主 AI 可见评价与绘图交互

V1.4.345 在 V1.4.344 的复刻调度基础上，补齐了主 AI 的公开任务说明、逐张评价和下一步提示，并修正最终提示词复制边界。

## 本轮改动

- “复制最终提示词”现在只复制纯正向 Tag，不包含标题、负面提示词或格式包装。关闭负面提示词时，负面 Tag 也不会进入主 AI 工具输入、任务状态或界面。
- 删除复制按钮下面重复出现的空白输出框，候选图和最终提示词只保留一个有效输出块。
- 主 AI 在第一次调用绘图工具前，用当前界面语言说明任务和接下来要做的事；图片完成后通过 `generation.comment` 写入简短评价、差异和建议。
- 候选卡在图片下方显示主 AI 评价；下一行单独说明会继续修订、暂时交付审阅，或达到本轮出图上限并引导用户使用“按这张图继续优化”。评价会随任务保存并进入后续判断。
- 评价不启动额外视觉子代理，也不会暴露思维链。主 AI 可以结合自己看到的图片和必要的辅助意见自主决定是否继续生成或选图。
- 任务在达到默认工具回合上限时，如果已经选定候选，不再额外发起一次模型请求，避免出现任务已完成但请求因预算耗尽而报错。

## 检查与使用

- `npm run check`：710 项通过，0 失败；包含语法、架构护栏、单元测试和回归检查。
- 未执行付费 API、真实图片生成、真实 ComfyUI 或慢速 Electron 启动测试；这些仍需用户人工验收。
- 桌面包复用上一测试版的 Electron 运行库、依赖和模型文件，打包时会校验源码副本、`resources/app.asar`、依赖、模型和版本化 exe。
- 双击 `AI绘画Tag工具箱V1.4.345.exe`。本轮未 push、未发布 Release。

主 AI 可能仍会误判图片内容；本轮改动确保判断过程有可见记录，并使评价能作为后续任务输入。旧存档继续保留原有恢复兼容路径。
