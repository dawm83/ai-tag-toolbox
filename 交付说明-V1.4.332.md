# V1.4.332 · WD 通用标签分类

`wd_general` 是 WD 识图标签文件中类别编号 0 的通用集合，并不是按内容划分的工具箱分类。此前缺少细分类的词会留在这里。

本版将其中 2144 条标签分配到 21 个现有大类、101 个现有子分类。例如：飞机进入“场景与环境 / 城市交通”，吊带背心进入“服装 / 上衣”，发圈进入“道具与装饰 / 发饰”，骑行及站坐动作进入“动作与姿势”。

112 条明确成人内容进入成人标签，并关联成人开关；已有的用户成人标记覆盖仍然优先。总标签数保持 53,707，原始 Tag、ID、中文显示名和角色引用未改变，收藏继续使用原条目。

首次启动会使用已验证的内置库升级映射保存用户覆盖文件，沿用原有备份流程；明确手动改过的分类、名称、备注、搜索资格等字段会保留。空的通用桶从侧栏隐藏，若有手动保留其中的条目，仍会显示“识图通用词”。本轮没有直接写入当前用户数据。

## 验证

- `npm run check`：617 项测试全部通过，含整库分类覆盖、原文/引用保留、旧版升级、自定义分类保留及成人筛选。
- `node scripts/build-unified-tags.mjs --check`：四个既有产物及分类报告与构建结果一致。
- 读取当前用户词库后在内存副本执行升级：通过；真实文件字节未改变。
- 桌面包按完整清单和 SHA-256 验证，包含运行库、语言资源和模型；未运行启动应用的慢测试，真实桌面交互由用户人工验收。

分类依据原始英文含义和现有分类体系。原中文译名仍有历史翻译歧义，本轮不批量改名；少数多义、作品专属词的归类可随使用反馈调整。完整对应表和统计见 `assets/数据资产/标签/wd-general-taxonomy.json` 与 `wd-general-classification-report.json`。

运行 `AI绘画Tag工具箱V1.4.332.exe`。未推送、未发布 Release。
