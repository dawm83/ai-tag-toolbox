# V1.4.335 · Agent 角色 Tag、续轮与 UI 修正

## Agent 与生图流程

- 用户已经提供完整“参考 Tag”时，任务直接把整段 Tag 交给文生图子代理，跳过角色库查询和角色选择；只有只给名称、资料不完整或存在歧义时才进入角色查询。
- 继续优化会把用户点评作为新一轮优化输入，清除旧轮的重复提交锁、最终选择和旧自动预算，避免下一轮继续沿用上一轮图片提交状态。
- 主 AI 固定规则加入默认界面语言约束：中文界面下使用中文解释和任务状态，Tag、角色名、作品名和代码保留原文。

## UI

- 文本选中颜色改为浅色/深色主题都清晰可见。
- 对话输入、编辑框和候选反馈区禁用无效的原生拖动，保留文本选择与复制。
- 最终提示词增加独立复制按钮；候选图也显示可复制的实际提示词，并保持正负提示词换行。

这版调整 Tag-only 结果的显示方式。生成结果不再拆成大量 Tag 卡片，而是展示为一个 `text` 代码块：正向 Tag 保持在第一段，存在负向提示词时以 `Negative prompt:` 分隔。代码块顶部的复制按钮会一次复制全部内容，方便直接粘贴到绘图工作流或继续交给 AI。

## 验证

- `npm run check`：640 项测试全部通过。
- 新增角色 Tag 直传、续轮预算重置、最终提示词复制、文本选择/拖动和语言规则回归测试。
- UI 回归覆盖：代码块语言标记为 `text`、只存在一个代码块、没有 Tag 卡片、复制内容同时包含正向和负向提示词。
- 普通 Markdown 代码块和候选图的“查看本轮 Tag”区域未改动。
- 未执行慢速 Electron UI/smoke 测试；真实模型和 ComfyUI 交由用户人工验收。

运行 `AI绘画Tag工具箱V1.4.335.exe`。本轮未推送、未发布 Release。
