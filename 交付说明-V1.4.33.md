# V1.4.33 快捷收藏页测试版

基于 V1.4.32，新增独立的快捷收藏工作区。运行桌面目录中的 `AI绘画Tag工具箱V1.4.33.exe`。本次为本地测试交付，没有推送或发布 Release。

## 功能

- 多系列横向并列，每列内部竖向排列单标签和标签组；支持子分类、置顶、排序、折叠和快捷定位。
- 单标签沿用现有风格；标签组显示组合图标、名称及内容预览，系列颜色同步标题、定位按钮和条目。
- 直接复制、就地编辑名称/说明、侧栏编辑完整原文/备注，支持自动保存和上一条/下一条。
- 收藏内部搜索只查收藏，支持系列、子分类、类型筛选及命中高亮；定位后可返回搜索结果。
- 自录内容默认参与主 Tag 页及 AI `tags.search` 查询；关闭后仍可内部搜索。备注只参与内部搜索。
- 系列自动配色、自定义颜色和多系列批量改色；内容缩放 75% 至 150%、列宽和显示偏好单独保存。
- 批量移动、复制、删除、置顶及设置可查询性；30 步撤销/重做；最近复制记录。
- 支持逐行/TSV 粘贴与 JSON 文件导入导出；导入先预览，覆盖导入明确提示影响并提供备份。
- 收藏选择独立保存快照，底部可展开查看；修改或删除收藏不改写已经加入的 Prompt。

## 数据与实现

继续使用原 JSON Storage，没有新增数据库、框架或网络服务。业务集中在 Favorites，格式转换独立为纯函数模块，页面只维护草稿和显示状态。已移除旧收藏抽屉与 Assistant 的重复收藏接口。

旧 `rewrite_favorites` 只读迁移到新收藏架，保留原键；词库无法解析的内容保留原值，异常旧记录保留并提示修复。新数据使用 `favorites_shelf_v1`、`favorites_selection_v1` 和 `favorites_recent_v1`；显示偏好使用 `favorites.view`。

原文不会为搜索或复制被重写；撤销只记录受影响记录。排序结果按 revision 缓存，返回页单独复制，避免每次读取列都复制整个收藏库。

## 自动验证

必跑 `npm run check`，涵盖源码语法、21 条正确性规则、架构护栏、单元与 jsdom 测试。另从 Git 导出到干净目录执行 `npm ci` 和同一检查。最终提交号、测试数量、源码与 asar 校验、运行时依赖及模型保留信息以包内 `build-info.json` 为准。

重点覆盖：旧数据迁移、精确原文、内外部查询隔离、超过 500 条的分页、编辑切换与输入法、保存失败恢复、导入原子性、批量撤销、底部快照以及关闭前的保存顺序。

性能脚本 `node scripts/benchmark-favorites.mjs` 测试 3,000 / 10,000 条收藏（20 系列、每系列 5 子分类）。本机为 Windows x64、Node v24.19.0、Ryzen 7 9800X3D。初步优化把两组收藏架数据读取从约 593 / 2,160 ms 降到约 7 / 19 ms；这些是纯数据耗时，不代表 Electron 首屏或滚动帧率。

加 `--view` 可运行 jsdom 首批构造基准。当前首批为 240 条，节点数从约 6.5 万至 7.1 万降到约 0.8 万，3,000 / 10,000 条数据下首次构造约 163 / 180 ms；浏览时继续加载列，定位会主动载入目标。这只反映 DOM 构造，不含 Chromium 样式、布局与绘制。

## 人工验收

1. 打开快捷收藏，检查原收藏是否出现；创建系列、子分类、单标签和标签组。
2. 粘贴带空格、权重、转义和换行的内容，复制到文本编辑器核对；快速修改后切换条目或关窗，再打开检查。
3. 内部搜索备注，定位原处，再返回结果；关闭全局查询后检查主页面不再返回该收藏，内部仍可查。
4. 加入收藏组到底部后修改原组，确认底部仍保持原快照；取消、重选和清空均正常。
5. 批量移动、改色、删除后撤销；导出 JSON 后导入；检查带引号/多行单元格的 TSV。
6. 在 980、1360、1920 宽窗口检查深浅主题、75%/100%/150% 缩放、拖动、系统剪贴板、文件选择和下载。

按项目要求没有启动 Electron 慢测，没有调用真实 AI/ComfyUI。界面像素布局、系统行为及真实模型结果仍需本轮人工反馈；不以 DOM 测试代替这些验收。
