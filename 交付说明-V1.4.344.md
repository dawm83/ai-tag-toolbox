# V1.4.344 · 主 AI 视觉判断与自主调度

基于 V1.4.344 主AI视觉判断与自主调度版本，保留已有性能优化。

## 已完成

- 主模型收到真实参考图；原图与候选可一起重看，同批看图请求合并。明确不支持图片时降级为文字调度，视觉子代理提供观察依据。
- 主 AI 自主选择元数据、本地 Tagger、视觉辅助、生成/修改 Tag、单轮出图、评价及选图。已有 Tag 可直接出图，不必再经过 AI 重写。
- 本地 Tagger 仅在初始阶段运行，复刻首轮强制优先使用一次本地 Tag；缓存成功结果并标为弱提示，后续修改不重新识别，缓存也按会话授权检查。
- Tag 子代理的模型消息只保留关键要求、可选参考图、上一版完整 Tag 和本轮修改意见；不转发旧蓝图、完整评价 JSON、历史反馈和角色档案。
- 新任务每轮图像都返回主 AI 判断；小差异定向修改，大差异可重新描述后编译。普通反馈和候选卡续改保持原 job 与所选候选基线。
- 角色选择完成后先交回主 AI，确认身份并准备 Tag 后才出图；连接恢复不重新提交已接收的 ComfyUI 请求。
- 原始要求、生成次数、取消、会话图片权限、候选与实际提示词的对应关系由程序维护。
- 完全匹配旧内置主提示词的文本自动升级；自定义提示词和 Tag 编译知识保持。主 AI 选择的结果不伪装成辅助评分通过或“硬错误为零”。

## 检查与使用

- `npm run check`：701 项通过，0 失败；语法、lint、架构护栏和回归检查通过。
- 已有及新增测试覆盖真实调用链，外部模型、Tagger、ComfyUI 使用受控网关；未执行付费 API、真实出图或慢启动测试。
- 桌面包复用 V1.4.342 的 Electron 运行库和模型，打包时核对源码副本、asar、依赖和对应版本 exe。详细结果见包根 `build-info.json`。
- 双击 `AI绘画Tag工具箱V1.4.344.exe`。本轮未 push、未发布 Release。

## 人工验收

请用新对话测试原参考图：观察是否读取内置 Tag／本地识图一次、首轮是否使用初始 Tag、反馈后是否沿同一任务改词。再测试文字主模型搭配独立视觉 API，以及候选卡续改。

模型可能仍然误判图片；这次修复的是图片输入、证据利用、调度和上下文边界。旧存档任务继续保留原有恢复方式；新的自主调度用于新建任务。已绑定参考图节点的工作流仍沿原配置使用图片，交付中的 recreationMode 标明实际路径。断线时绘图开关的既有行为未在本轮修改。


## V1.4.344 交互闭环补充

- 候选卡反馈输入框独立显示，明确当前候选和主 AI 决策状态。
- 主模型视觉能力支持自动/确认支持/确认不支持三态设置。
- 任务时间线显示本地 Tag、基线、对照、差异、Tag 修订和候选选择。
- 复刻没有可靠内置 Tag 时，首轮强制先运行一次本地识图并原样出图；后续由主 AI 对照和修订。
- `npm run check`：701 项通过。
