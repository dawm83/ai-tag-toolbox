# V1.4.355 · 首次启动与版本查询提示

- 业务 EXE 不再以隐藏窗口标志启动；创建窗口后主动显示并聚焦。启动器收到就绪心跳后退出。
- GitHub 查询失败时展示当前界面语言的网络提示，不显示 Electron 的远程调用前缀。已安装版本仍可选择。
- 查询成功但没有兼容发布包时说明“暂未发布支持在线更新的版本”；没有更高兼容版本时说明“已是最新版本”。

桌面入口：`AI绘画Tag工具箱V1.4.355/AI绘画Tag工具箱.exe`。运行槽位在 `versions/V1.4.355` 中；图库、会话与设置继续使用 `%APPDATA%\ai-tag-toolbox-rewrite`。

检查包括启动器参数、首次窗口显示、Electron 包装错误、中英提示、离线版本列表，以及 `npm run check`。最终计数见桌面 `build-info.json`。

需人工确认首次双击的窗口表现及实际网络连接。界面错误提示修复不等于 GitHub 网络已恢复。本轮未推送、未发布 Release。
