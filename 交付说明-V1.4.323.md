# V1.4.323 · 修复首次启动迁移报错

修复 V1.4.322 首次启动时可能出现的 `INVALID_LEGACY_INPUT`。

根因是初始化期间，设置/提示词模块重写 `rewrite-storage.json`，旧标签迁移的前后校验同时读取该文件。原写入方式会先清空目标，迁移恰好读到零字节或半份 JSON 时被误判为旧数据非法。

现在先写同目录临时文件，完成后再替换目标文件。读取者始终拿到完整的旧版本或新版本；写入失败保留原文件，现有保存失败提示与重试继续生效。没有放宽旧数据校验，也没有删除、清空或改写个人收藏。

验证：先以真实安装包 preload 和独立临时用户目录复现零字节读取，再用修复后的完整 preload 验证 188 条模拟收藏正常迁移。另有并发读取、替换失败保留旧文件及重试的自动回归。完整检查和打包记录见包外 `build-info.json`。

本轮只读检查了用户旧数据的结构和条目数量，并在内存中核对迁移保留数量；未写用户业务目录。升级后关闭旧版，再启动 V1.4.323；无需删除任何数据文件，首次迁移仍会保留备份。

保留 V1.4.322 的统一词库、中文分类、搜索及 ComfyUI 修复。未推送或发布 GitHub。
