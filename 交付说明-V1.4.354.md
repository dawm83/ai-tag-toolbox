# V1.4.354 · 版本更新与回退测试版

## 本轮内容

- 左上角版本号打开版本管理面板，可读取固定 GitHub 仓库的 Release 列表，并区分当前版本、已安装版本和可下载版本。
- 更新包下载到临时目录后，按版本资产名校验 ZIP、SHA-256 和 `build-info.json`，再解压到新的版本槽位。
- 稳定启动器读取 `version-state.json`，启动 `versions/V<版本>` 中的业务包；目标版本启动未写入心跳时自动回退到上一版本。
- 版本切换前将共享用户数据备份到 `%APPDATA%\\ai-tag-toolbox-rewrite\\backups`。图库、会话、设置、提示词和图片本体不复制进版本包，也不因切换自动删除。

## 桌面包结构

```text
AI绘画Tag工具箱/
├─ AI绘画Tag工具箱.exe
├─ version-state.json
└─ versions/
   └─ V1.4.354/
```

`AI绘画Tag工具箱.exe` 是稳定入口；业务 EXE、`resources/app.asar`、模型和运行依赖位于版本槽位中。

## 验证范围

- 已运行 `npm run check`，包括源码护栏、单元测试和回归测试。
- 已做结构化打包的静态校验：槽位版本、启动器、`app.asar`、源码镜像、模型、原生依赖、EXE 和 `build-info.json`。
- 尚需人工验证真实 GitHub Release 网络下载、取消下载、版本切换、启动失败回退、Windows 文件锁定，以及真实 AI/ComfyUI 运行。

本版本未推送 GitHub，也未发布 Release。
