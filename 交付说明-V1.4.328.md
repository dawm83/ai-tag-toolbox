# V1.4.328 · UI 反馈修正

基于 V1.4.327，逐项处理本轮四条反馈。

1. 恢复标签分类色与边框。分组顶部、卡片侧边、子分类按钮和侧栏选中态使用对应颜色；收藏页各列有独立彩色外框，保留自定义颜色。
2. 导航恢复接近 V1.4.326 的三行居中排列，品牌保留左侧。搜索/AI 页签独占中间行，功能按钮独占下一行，增大间距和点击区域。
3. 补齐 27 个内置分类的图标，包括身体、表情、眼睛、头发、服装、鞋袜、道具、姿势、场景、镜头、天气、光影、食物和动物等。“全部”及角色库也使用对应图标，自定义分类保留通用图标。
4. 加强导航、搜索、标签操作、收藏操作与 AI 清空按钮边框；深色模式使用独立的边框颜色。

同时修正分行导航下的样式菜单遮挡、AI 页签被旧规则压窄，以及较矮窗口中翻译输入区重叠的问题。

## 检查

- `npm run check`：语法、lint、架构护栏、605 项测试和 `regressions-v194` 全部通过。
- 本地图标 SVG 解析正常；全部内置分类的映射均指向实际存在的图标。
- 隔离页面预览核对浅色/深色、1280 × 720 和 980 × 680 窗口，检查标签/收藏颜色、侧栏上下半段图标、导航间距、样式菜单点击、AI 页签宽度、翻译区边界。
- 按项目规则跳过启动应用的慢测试；真实 Electron 窗口、Windows 缩放比例、真实用户数据与完整业务流程仍待人工测试。

桌面运行文件：`AI绘画Tag工具箱V1.4.328.exe`。本版未推送、未发布 Release；包根目录 `build-info.json` 记录源码提交及打包核验。
