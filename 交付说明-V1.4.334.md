# V1.4.334 · Agent 意图路由第一阶段

这版先落实“请求什么就走相应流程”。明确搜索 Tag 时只搜索；明确分析图片时只定位图片并识图；生成 Tag 时固定为 Tag 输出，即便 ComfyUI 已连接也不会因此出图。生成和分析阶段使用当前用户原话，避免主 AI 在转交子代理时替换要求。

工具结果返回后，主 AI 进入整理答复阶段；重复调用和越界调用不会执行。已有生成任务返回失败后也进入解释阶段。工具错误仍返回模型，允许修正合法参数；同轮暂停任务会补齐全部 tool 回执。

对话中的“任务过程”增加本轮任务类型、跳过工具、等待补充信息及整理答复记录。前轮顺序多工具调用和失败次数限制一并包含在本包中。

## 人工试用

1. “帮我找找与蓝发有关的 tag”：查看任务过程，应只有搜索与答复，不能出图。
2. 附图后输入“分析这张图片，重点看构图”：应只定位/识图及回答。
3. “帮我生成蓝发白裙女孩的 Tag，不要出图”：即使启用 ComfyUI，也应只返回提示词。
4. “如何用 ComfyUI 生图”：应回答使用方法，不执行渲染。
5. “画一个蓝发女孩”与“复刻这张图片”：沿用现有生成编排器；结果后不重复启动整套任务。

## 检查与限制

- `npm run check`：636 项通过，包括新增路由、越界阻止、原话保留、否定和混合表达、收尾、原生工具记录和 DOM 事件显示。
- 未执行慢 UI/smoke/i18n 启动测试，未使用真实 AI 或 ComfyUI 接口；桌面可执行文件及打包完整性另由 `build-info.json` 记录。
- 本地规则只锁定能明确识别的请求。陌生表达、混合任务和“继续”等上下文请求仍由主 AI 判断，不宣称已实现通用语义理解。
- 生成 Tag 当前仍复用已有 tags-only 编排分支。带图分析的真实载荷校验、失败撤回、本地 Tag 直出复刻首轮、多轮差异修订及完整报告属于后续阶段。
- 还未改变 ComfyUI 连接失败关闭绘图的旧行为；没有加入新的服务、依赖或复杂事务。

运行 `AI绘画Tag工具箱V1.4.334.exe`。本轮只交付第一阶段，人工试用后再推进下一条主线。未推送、未发布 Release。
