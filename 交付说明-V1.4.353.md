# V1.4.353 · 修复反馈轮迭代预算

本版修复用户在已有候选图上提出新修改时的迭代预算问题。此前 `beginFeedback()` 只给旧任务增加一轮额度；当主 AI 完成第一张修改图后，评价工具会把继续修改强制改成“本轮出图次数已用尽”，即使用户刚刚开启了新的修改轮次。

现在每次新的用户反馈都会按当前任务配置的 `maxAutoRounds` 获得完整预算。历史候选和累计轮次继续保留，主 AI 仍能对照之前的结果；只有新的反馈轮确实消耗完预算时，才显示次数上限提示。主 AI 选择交付时保留正常交付文案，不会被改写成上限提示。

检查：`npm run check`，730 项通过，0 失败；`git diff --check` 通过。真实模型、ComfyUI 和 Electron 启动仍交由用户人工验收。

本轮未 push、未发布 Release。
