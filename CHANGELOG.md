# 更新日志

## V1.4.339（本地内部测试版）

- 多候选任务的普通文字反馈必须明确指定候选图；即使任务有默认选中候选，也不会替用户猜测目标。
- 新增默认选中候选的歧义路由回归测试。
- `npm run check`：660 项测试全部通过；真实模型和 ComfyUI 交由用户人工验收。

## V1.4.338（本地内部测试版）

- 用户在上一轮生成后直接提出姿势、服装或构图修改时，反馈会回到原生成任务，保留原始要求、参考图、当前 Tag 和候选历史。
- 自动生成完成后也可以继续优化；多候选且未明确目标时先提示选择，不重新调用主 AI 从头规划。
- 只有明确要求重新识图时才再次调用识图；姿势反馈不能删除角色身份、外貌、服装、场景等未修改内容。
- 候选卡的“继续优化”直接提交原任务；只要 Tag 时不检查或调用 ComfyUI。
- `npm run check`：659 项测试全部通过；真实模型和 ComfyUI 交由用户人工验收。

## V1.4.336（本地内部测试版）

- 角色库在 Tag 编译和修订阶段只提供角色身份、作品归属和身份 Tag，不再把默认服装、袜子、发色、眼色或配件注入提示词。
- 用户当前要求和复刻图片中的目标修改继续作为生成依据；完整角色外观资料仅保留给候选图评估使用。
- 增加角色外观污染回归测试，并更新固定 Tag 子代理的边界提示。
- `npm run check`：641 项测试全部通过；真实模型及 ComfyUI 交由人工测试。

## V1.4.335（本地内部测试版）

- Tag-only 结果改为单个 `text` 代码块展示，正向 Tag 和负向提示词集中在同一块内容中。
- 复制按钮一次复制全部可用提示词，不再把最终 Tag 拆成大量可点击卡片。
- 用户已经提供完整角色 Tag 时跳过角色库查询和选择；只提供名称或存在歧义时才查询角色。
- 继续优化开启新的轮次预算和提示词提交上下文，不重复沿用上一轮图片提交锁和最终选择。
- 修正文本选择颜色、无效拖动、候选最终提示词复制和中文界面下主 AI 的语言约束。
- 保留普通 AI Markdown 代码块和候选图 Tag 展示不变。
- `npm run check`：640 项测试全部通过；真实模型及 ComfyUI 交由人工测试。

## V1.4.334（本地内部测试版）

- 明确查 Tag、分析图片、生成 Tag、问答、翻译、生图和复刻任务时，只向主 AI 提供相应工具，并在执行前拦截越界调用。
- 短任务成功后只整理答复，禁止模型重复执行工具；生成任务返回失败后停止重新启动整条流程。
- 明确只要 Tag 或不要生图时固定 tags 输出；生成和分析任务把用户原话交给相应子代理。
- 模糊和混合请求保留上下文判断，继续兼容现有高层生成和恢复流程；当前不变更图片传输和复刻首轮算法。
- 任务过程显示分类、跳过的工具、暂停与答复阶段。同轮多个调用暂停时补齐回执，避免下轮接口因缺结果出错。
- 包含前轮 Runtime 的顺序多工具调用、错误回传及重复失败次数限制。
- `npm run check`：636 项通过；真实模型及 ComfyUI 交由人工测试。

## V1.4.332（本地内部测试版）

- 将 `wd_general` 中 2144 条通用识图标签按逐条 ID 对应表分到 21 个现有大类、101 个现有子分类。
- 保留全部原文、ID、中文显示名、收藏及角色关系；112 条明确成人内容归入成人分类并受成人开关控制。
- 提供从原始及 V1.4.322–331 内置库到新版的升级映射，保留用户明确修改的字段和分类。
- 空的 WD 通用桶不再出现在侧栏；保留在旧分类中的用户条目仍可访问，名称显示为“识图通用词”。
- `npm run check`：617 项测试全部通过；构建产物可重现，当前用户词库的只读升级模拟通过。

## V1.4.331（本地内部测试版）

- 移除已收藏 Tag 卡片右下角的收藏位置箭头及其弹出菜单，取消预留间距。
- 保留常亮金色星标、再次点击确认取消收藏的交互，以及已有收藏关系。

## V1.4.330（本地内部测试版）

- 已收藏标签显示常亮的金色实心五角星，再次点击弹出取消确认；取消操作保留收藏，保存失败保留原状态。
- 主页取消确认列明受影响收藏位置；收藏页星形入口只取消当前组内的收藏。标签内容、角色引用与当前选择保留。
- 保留收藏位置跳转，并增加“收藏到其他位置”入口；角色身份收藏同步星标状态。
- 搜索框最大宽度收窄为 560 像素，保留最大化单排、还原后三排的导航布局。
- `npm run check`：610 项测试全部通过。

## V1.4.329（本地内部测试版）

- 导航跟随 Electron 的最大化/还原状态切换：最大化为单排，普通窗口保持三行居中。
- 最大化且逻辑宽度较小时收紧偏好按钮与 AI 页签，保留操作名称和悬停说明；页面重载保持正确窗口布局。
- 同步预加载桥的版本标识；新增最大化、还原及重载状态测试。

## V1.4.328（本地内部测试版）

- 恢复分类色：侧栏选中项、标签分组、标签卡片和子分类使用对应系列色与边框；收藏列使用各自颜色和独立外框。
- 导航恢复接近旧版的三行居中排列，搜索与功能按钮分行；AI 页签保持完整宽度。
- 为全部 27 个内置分类及“全部”配置对应的本地图标，自定义分类继续使用通用标签图标。
- 加强导航、搜索、标签操作、收藏编辑与 AI 清空按钮的边框，保留深浅主题。
- 修正三行布局下的下拉菜单遮挡与翻译输入区重叠；不改变数据和 AI 调用链路。
- `npm run check`：605 项测试全部通过；桌面完整流程待人工验收。

## V1.4.327（本地内部测试版）

- 参考 shadcn/ui Dashboard 与 Linear，统一浅色/深色工作台配色、边框、间距、按钮和输入控件。
- 常见桌面宽度使用两行导航；主要入口、分类及标签操作采用本地 Lucide 图标。
- 标签卡片改为规则网格，编辑/收藏/复制操作与标签文字分区，保留键盘焦点反馈。
- 新增标签与位置弹窗统一样式，保存/取消及验证错误固定显示；修正浅色错误文字对比度。
- AI 页增加中英文欢迎状态，调整输入区、API 配置卡片、收藏、角色、翻译和图片库样式。
- 保留现有业务与本机数据；按项目要求执行 npm run check，桌面完整流程由用户人工验收。

## V1.4.316（本地内部测试版）

- 移除角色库列表行右侧悬停显示的星形收藏按钮及样式，保留条目点击查看详情。


## V1.4.315（本地内部测试版）

- 收藏编辑面板新增红色“删除收藏”，通过页内二次确认删除当前条目。
- 允许删除原文被清空的已保存收藏；等待正在进行的保存结束，避免删除与自动保存冲突。
- 删除失败明确提示并允许重试；取消确认保留当前输入，删除后仍可继续新增标签。


## V1.4.314（本地内部测试版）

- 角色库和主页 Tag 增加用户编辑与收藏入口，修改以本机覆盖层保存，不改写内置数据。
- 修改结果提供高亮标记、恢复默认和有限历史记录。
- 角色收藏保留 sourceCharacterId，可继续追溯角色来源。


## V1.4.313（本地内部测试版）

- 删除 Tag 展开入口；收藏不再区分单标签和标签组，内容编辑只保留 Tag、名称、备注，保存成功后关闭面板。
- 编辑器的收藏页和标签栏选项与实际导航同步；旧无归属记录移入本页第一个真实标签栏，删除标签栏会保留并转移其中内容。
- 标签栏导航增加眼睛按钮，可隐藏/恢复列并记住显示状态。
- 收藏页页签增加关闭按钮；左侧文件夹列出全部页面，支持恢复关闭的页面和确认删除。
- 删除确认改为页内弹窗；关闭/切换页面先保存有效草稿，保存失败则保留输入。
- 清理旧未分类、导入工具条和批量工具条遗留的视图代码。


## V1.4.39（本地内部测试版）

- 修复删除收藏页或标签栏后，旧的右键菜单焦点和右侧快速编辑器残留，导致新增标签输入框无法点击的问题。
- 新增删除页面后重新创建标签的回归测试。


## V1.4.38（本地内部测试版）

- 修复点击空白卡片新增标签后焦点被按钮抢回，导致无法直接输入的问题。
- 新增回归测试覆盖真实点击后的焦点恢复与 Tag 输入。


## V1.4.37（本地内部测试版）

- 删除收藏页标题控制行，删除旧工具栏；页面下方直接进入标签栏页签和标签列。
- 收藏页与标签栏统一使用右键菜单，菜单只有“编辑”和“删除”；编辑弹窗统一修改名称与颜色。
- 缩放控制并入收藏页页签行右侧，继续保留缩放功能。


## V1.4.36（本地内部测试版）

- 移除收藏页旧工具栏中的新增标签、组合、撤销、重做、最近复制、批量管理、导入导出、显示模式和列宽控制，只保留缩放。
- 默认缩放调整为 120%；已有旧版 100% 偏好首次进入时迁移到 120%。
- 子标签栏列改为固定宽度，新增列不再拉伸占满整个收藏区。
- 普通 Tag 点击复制后显示“已复制”，行为与主页 Tag 一致。


## V1.4.35（本地内部测试版）

- 收藏页改为工作簿式三层结构：顶部收藏页页签、第二排子标签栏页签、下方当前收藏页的多列编辑区。
- 空收藏自动创建“新建收藏页”和“新建标签栏”；页签支持新增、切换、拖动、右键重命名、改色和删除确认。
- 子标签栏支持新增、拖动交换、重命名、改色和删除；每列保留一个空白 Tag 卡，点击后只需输入原文、名称和可选备注。
- 标签备注改为悬停提示；右上角关闭按钮可关闭轻量编辑器；保留主页 Tag 的复制、选择和编辑行为。
- 收藏数据格式保持兼容，暂不恢复收藏页内部搜索。


## V1.4.34（本地内部测试版）

- 收藏页顶部切换系列，一次显示一个系列；名称和颜色可直接修改。
- 子分类横向分列，列内纵向显示 Tag；默认全部显示，支持逐列隐藏和全选/取消全选，显示偏好按系列保存。
- 暂时移除收藏页内部搜索和旧筛选/折叠逻辑，保留主页全局查询与收藏跳转。
- 每列独立分页和按需加载；排序与勾选移入批量管理，保留直接复制和编辑。
- 已导入收藏沿用原数据结构；新增/切换系列保存当前草稿，隐藏子分类不删除内容。

## V1.4.33（本地内部测试版）

- 新增快捷收藏页：多列收藏架、系列与子分类、单标签/标签组、自由原文、说明和备注。
- 支持内部搜索、命中定位、缩放、配色、就地编辑、批量管理、撤销重做和最近复制。
- 收藏原文与底部选择快照独立；主页面和 AI 查询只返回允许参与全局查询的收藏，不返回个人备注。
- 提供 TSV/逐行粘贴和 JSON 导入导出；旧收藏保留原键并迁移，修复磁盘失败恢复、Windows TSV 行号及快速编辑保存问题。
- 收藏排序按数据版本缓存，页面分批构造内容；新增数据域及真实模块页面联动回归。桌面布局与系统行为等待人工测试，本版未推送或发布。

## V1.4.32（内部测试版）

- 角色确认卡新增“这是原创人物，继续”，无需从角色库选择即可恢复原任务。
- 原创选择只跳过当前待确认人物，保留原始绘图要求和已确认的其他角色；支持重开后继续，拒绝过期或跨会话操作。
- 主 AI 默认及固定调度规则明确区分原创人物、OC、自设和已有作品角色，原创名称、普通外貌描述无需进入角色库确认。
- 覆盖无候选、多候选、多人、恢复、防重复点击及失败恢复；真实模型是否正确识别原创意图仍需人工验证。

## V1.4.31（内部测试版）

- 修复 thinking 模式下多轮工具调用因缺少 `reasoning_content` 返回 HTTP 400 的问题。
- 工具调用和最终回答按轮保留原始推理内容，流式/非流式请求、会话保存、恢复及导入后继续对话均完整回传。
- 区分显式空推理与没有推理字段的普通响应；兼容没有 transcript 的旧会话中已保存的 reasoning。
- 新增 8 项回归用例，涵盖原始 400 复现、空白保留、普通模型兼容和会话恢复。
- 旧版已丢失的逐轮推理无法补回，旧对话若仍报同样的 400，请新建对话验证。

## V1.4.3

- 以 GitHub V1.4.191 为基线完成角色库、统一 Agent Runtime、ComfyUI 配置档和自动/手动生图链路升级。
- 新增 34,122 个离线角色资料，角色专用词独立保存并在角色确认后作为可选参考交给 Tag 子代理。
- 新增候选图评价、轮次横排、最终选择抢占式结束、重复提示词拦截，以及 1–10 的批量数量和 ComfyUI 调用上限同步。
- 新增脱敏调用监视器、对话图片清理、独立图库引用和复杂 ComfyUI 工作流的显式节点绑定。
- 翻译页新增稳定 ID 双向对照、高亮选区、标点缺口恢复和 AI 翻译加载状态；翻译请求关闭思维链和流式输出。
- `npm run check` 共 172 项通过。

## V1.4.191

- 修复大尺寸图片上传导致的调用栈溢出，图库、对话图片与 Comfy PNG 工作流共用安全解码链路。
- 兼容 Wildcard、Primitive 与间接 Latent 节点工作流，并用 D 盘 17 份 ComfyUI PNG 验证导入与参数覆盖。
- 删除设置页重复的 ComfyUI 出图开关，修复 API/Comfy 表单保存、模型异步回写、连接结果判断与工作流回填。
- 首次启动可从旧版 Electron 用户目录恢复 API、Comfy 与项目配置，且不会覆盖新版已有的非默认设置。
- 本地 OpenAI 兼容服务优先使用标准 `/v1/models`，保留 Ollama 旧模型接口回退。

## V1.4.190

- 修复 ComfyUI 出图开关被异步状态刷新取消的问题，并恢复跨版本 portable 配置。
- 修复图库和右侧识图图片上传入口，统一使用安全图片存储接口。
- 图片库页面仅保留上传、下载和删除操作，移除导入/导出控件。

## V1.4.189

- 完成独立图片库、会话图片仓库、Vision 单槽临时输入与精简 JSON 调用协议的整合。
- 修复连续对话中的图片重复发送、孤立工具消息和 HTTP 400 风险；增加会话/图片引用、缓存、取消与权限边界保护。
- 收紧 Vision 地址与 data URL 校验、图库删除引用计数、AI 上下文脱敏和兼容调用标记解析。

## V1.4.188

- 新增独立图片库页面、拖拽引用和图库清单导入导出。

## V1.4.184

- 新增图片仓储数据层、旧图片关联迁移与生命周期保护。
- 修正两阶段旧会话迁移和孤立图片物理删除，统一项目可见版本标识。

## V1.4.182

- 识图右侧抽屉动态避开顶部导航栏，改为经典“导航栏 + 右侧面板”布局。

## V1.4.181

- 识图接口拒绝推理字段时自动兼容回退，仍保持非流式最终输出。

## V1.4.180

- 赞助作者按钮改为更鲜艳的橙红色主题，保持统一导航按钮模板。

## V1.4.179

- 搜索按钮缩小并内缩 2px，让搜索栏自身的外框完整露出。

## V1.4.178

- AI 识图关闭流式思维链，更新为详细、精炼且如实包含 NSFW 的描述提示词。

## V1.4.177

- 成人标签使用粉色主题；搜索按钮与搜索栏融为无边框半圆端；重新生成图标调整为不裁切的统一操作按钮。

## V1.4.176

- 搜索按钮改为与输入框融合的右侧半圆弧形；精度选择更紧凑，并用竖线与搜索按钮分隔。
- 复制、修改、重新生成按钮统一为同一横向图标模板。

## V1.4.175

- 重新生成与复制按钮改为同一横向消息操作模板，并增强深色模式下复制/操作按钮的强调效果。

## V1.4.174

- AI 输出下的重新生成与复制按钮改为同一横向操作行并常驻显示；重新生成图标微调位置。
- 提高选中态对比度，修正浅色侧栏选中项强调条的亮度和左边框对齐。

## V1.4.172

- 完善对话操作布局：用户仅显示复制/修改，AI 仅显示复制并提供独立的重新生成按钮。
- 对话消息支持安全的 Markdown、表格、代码块和链接渲染。

## V1.4.171

- 重做对话消息布局：用户消息使用高对比右侧气泡，用户仅保留复制/修改，AI 输出保留复制并将重新生成独立放在输出下方。
- 增加安全 Markdown 渲染，支持标题、段落、列表、引用、表格、行内代码、代码块和链接。

## V1.4.170

- 采用更克制的 Indigo/Blue 主色令牌，提升浅色与深色模式选中态文字对比度，并调浅浅色侧栏选中强调条。

## V1.4.168

- 修正成人标签按钮状态对应关系：开启显示 ● 成人标签：开，关闭显示 ○ 成人标签：关。

## V1.4.167

- 成人标签按钮改用对称的空心/实心圆符号：○成人标签：开 / ●成人标签：关。

## V1.4.166

- 修正标签页面缓存键遗漏数据修订号的问题，避免自定义标签变更后误用旧结果。

## V1.4.165

- 增加搜索按钮并支持回车提交；切换左侧分类时保留搜索框文本，便于再次搜索。
- 子分类按钮改为跟随当前主分类配色；标签列表增加分页缓存、宽松索引按需建立和离屏懒绘制。

## V1.4.155

- 将顶部导航按钮重构为统一状态模板，统一选中/取消选中行为和文案切换，并修复收藏抽屉再次点击关闭。

## V1.4.154

- 修复赞助链接外部浏览器跳转、导航按钮激活态和成人标签颜色，并消除侧边栏选中态重影。

## V1.4.153

- 按 Material 3 / Primer 的颜色角色重做按钮主题，修复深色模式浅底浅字，并改善对话框按钮对比度。

## V1.4.152

- 修复中等宽度窗口下成人标签与搜索栏重叠的问题，搜索/AI 模块在窄桌面窗口独占一行。

## V1.4.151

- 建立统一按钮模板并覆盖主要按钮、图标按钮、分段切换、菜单和标签控件。

## V1.4.150

- 品牌标志贴近左侧边缘，左侧按钮补齐悬停与按下动效。

## V1.4.149

- 缩小左上角品牌图标，恢复版本号显示，并优化中等窗口的顶部单行对齐。

## V1.4.148

- 降低赞助按钮视觉权重，并让成人标签按钮的开启/关闭状态明显区分。

## V1.4.147

- 修复任务过程中的通用事件把同一条提示重复显示两次的问题。

## V1.4.146

- 修复连续对话时把 UI 工具追踪记录误当成 assistant `tool_calls` 发送的问题。

## V1.4.145

- 采用三列顶部导航：搜索居中、左右按钮向搜索区对齐；右侧四个功能按钮统一尺寸并使用分色低饱和样式。

## V1.4.144

- 修复识图抽屉层叠上下文问题，遮罩只变暗背景，识图面板和顶部导航保持清晰。

## V1.4.143

- AI 页面真正隐藏搜索框后显示模式切换器，翻译页保留搜索栏，确保两者宽度一致。

## V1.4.142

- 识图抽屉挂到公共工作区，翻译页等非 AI 页面也能通过导航按钮正常打开。

## V1.4.141

- 重排顶部导航顺序，样式按钮移到搜索栏左侧，搜索与 AI 模块切换器保持同等尺寸。

## V1.4.140

- 识图面板改为顶部导航抽屉入口，移除右侧面板的展开/收起按钮，并移除帮助入口。

## V1.4.139

- 翻译页顶部按钮显示“返回主页”，品牌副标题只保留版本号。

## V1.4.138

- 记住用户上次选择的助手/绘图模式，切换页面或重启后不再强制回到助手模式。

## V1.4.137

- 后续追问不再把绘图返图作为 assistant 图片消息发送，避免接口拒绝。

## V1.4.136

- 为关闭思考参数增加多服务商兼容透传，并在不支持可选参数时自动回退重试。

## V1.4.135

- 识图复制按钮改用与界面显示一致的去重 Tag 列表，避免粘贴时整段重复。

## V1.4.134

- 翻译输入框关闭浏览器拼写检查，英文 Tag 不再显示红色波浪线。

## V1.4.133

- 翻译思考面板在流式更新时保留用户的展开/收起状态，可在思考过程中点击展开。

## V1.4.132

- 翻译 AI 请求改为直接输出模式：提示词禁止思维链、分析草稿和 Markdown，并关闭流式推理回传。

## V1.4.131

### 重构

- 简洁模块化重写，页面外观沿用 V1.4.2 的使用习惯，业务代码不再依赖旧版脚本链。
- 业务代码拆分为 `src/modules/`：`tags.js`、`images.js`、`vision.js`、`vision-service.js`、`translation.js`、`assistant.js`、`ai-runner.js`、`comfy.js`、`prompts.js`、`storage.js` 等。
- 新增 `agent-tools/`（外部 Agent 工具）、`assets/`（标签数据与提示词素材）、`scripts/`（校验脚本）。

### 功能与行为

- Vision 识图一次只处理一个明确的 `imageId`，`mode` 支持 `metadata`、`local` 和 `ai`；页面、主 AI 与外部 Agent 通过同一个 Calls handler 调用。
- AI 页面提供"助手"和"绘图"两种模式；ComfyUI 未连接或工作流无效时，`comfy.render` 不会下发给主 AI。
- Calls 提供动态 capabilities 与 Agent Runtime/Admin 分组，`ai-runner.js` 统一助手/绘图工具循环。
- 外部 Agent 默认只获得 Runtime 工具（Tag、单图 Vision、可用时 Comfy 渲染、提示词/Comfy 设置读取）；开启"允许外部 Agent 修改提示词和 ComfyUI 迭代设置"后才会出现 Admin 工具。
- ComfyUI 工作流提交前按采样器连接关系覆盖固定的正/负向 Tag、尺寸、steps、CFG 等节点；无法定位可写节点时返回明确错误。
- 本地识图与离线翻译模型不随源码入库（`models/` 已忽略），模型缺失时页面仍可打开，本地识图会显示不可用原因。

## V1.4.1

### 新增

- 增加中英文界面与多语言国际化适配。

### 优化

- 优化 API 调用逻辑与长耗时请求处理。
- 完善相关状态提示与错误诊断。

### 注意

- 本地识图与离线翻译仍需随程序保留 `models/` 目录。
