# V1.4.336 · 角色身份引用边界修正

## 角色与生成流程

- 角色库在 Tag 编译和修订阶段只提供角色身份、作品归属和身份 Tag。
- 默认服装、袜子、发色、眼色和配件不会自动进入用户的正向提示词；用户当前要求优先。
- 完整角色外观资料仍保留在候选图评估上下文，用于判断角色辨识度，不作为生成硬约束。
- 固定 Tag 子代理在消息组装时再次裁剪外观字段，避免直接调用路径绕过边界。

## 验证

- `npm run check`：641 项测试全部通过。
- 已覆盖角色编译、角色修订、复刻候选评估和直接 Tag 子代理调用路径。
- 未执行慢速 Electron UI/smoke 测试；真实模型和 ComfyUI 交由用户人工验收。

运行 `AI绘画Tag工具箱V1.4.336.exe`。本轮未推送、未发布 Release。
