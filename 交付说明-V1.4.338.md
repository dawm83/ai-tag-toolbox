# V1.4.338 · 原任务续轮与反馈边界

## 生成流程

- 用户在已有图片后直接说“姿势不对”“改成站立”等反馈时，系统复用原 `jobId` 和候选图，不重新创建生成任务。
- 原始要求、参考图、当前正负 Tag、角色资料和候选历史会继续保留；本轮反馈只作为增量修订。
- 自动任务完成后也能继续优化。多张候选没有明确目标时会要求先选择候选图。
- 只有用户明确要求重新识图时才再次调用识图；姿势反馈不会被识图结果覆盖。
- 只要 Tag 的续轮不检查 ComfyUI，也不会调用绘图。

## Tag 保护

- 姿势类反馈只允许修改姿势相关 Tag。
- 角色身份、发型、服装、场景和构图等未被要求修改的内容会保留。
- 无效修订会停在当前结果，并提示缩小修改范围，不提交破坏性的新图。

## 验证

- `npm run check`：659 项测试全部通过。
- 新增普通聊天续轮、已完成任务续轮、只生成 Tag、候选歧义、姿势 Tag 保护和候选卡直接续轮测试。
- 未执行慢速 Electron/smoke 测试；真实模型和 ComfyUI 由用户人工验收。

运行 `AI绘画Tag工具箱V1.4.338.exe`。本轮未推送、未发布 Release。
