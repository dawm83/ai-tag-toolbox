# V1.4.313 收藏页编辑与页签管理

本版完成用户确认的七项调整：Tag 卡片移除展开入口；不再要求选择收藏类型；内容字段统一为 Tag、名称、备注；保存成功自动关闭；编辑器只列出真实标签栏；导航眼睛控制列显示；收藏页的关闭、文件夹恢复与删除分开处理。

收藏页右键保留编辑名称与颜色，删除移入文件夹列表并二次确认。标签栏右键保留编辑和删除。关闭页面和隐藏标签栏仅改变显示状态，应用重开后仍保留。全部页面关闭后可从文件夹恢复，或点击加号新建。

无归属旧记录在打开对应收藏页时归入第一个真实标签栏；该页没有标签栏时创建“新建标签栏”。删除标签栏将其中记录移入同页第一个其他栏；没有其他栏时创建一个新栏保留数据。原有 Tag 文本、备注和记录 ID 保留，旧数据中的中文说明在名称为空时作为名称显示。旧 kind/zh/aliases 元数据为文件兼容保留，不再作为独立编辑字段。

验证：npm run check（语法、正确性规则、架构护栏、单元与 DOM 回归）；独立示例数据的静态浏览器布局核对。未运行 Electron --uitest/--smoke；Windows 实际输入、剪贴板和拖动由用户人工验收。未修改 AppData 中的个人收藏、图库、API 设置或对话记录。

手工验收：

1. 编辑现有 Tag，确认只有 Tag、名称、备注三个内容字段；保存后面板关闭，重新编辑可见保存内容。
2. 点击标签栏眼睛隐藏和恢复列；退出再进入收藏页，确认状态保留。
3. 点击收藏页右侧 ×，从文件夹列表重新打开，确认标签仍在。
4. 从文件夹删除测试页，分别测试取消和确认，再新增 Tag，确认右侧输入可用。
5. 删除测试标签栏，确认原有 Tag 移到其他真实栏；如果删除唯一一栏，确认记录保留在新栏中。

本版为本地内部测试版，不 push、不发布 Release。
