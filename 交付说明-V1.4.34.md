# V1.4.34 收藏页布局调整

本版把收藏页改为顶部切换系列、下方子分类分列展示。打开一个系列后，每个子分类独占一列，标题下方纵向排列单标签和标签组。

- 顶部系列栏切换当前系列；名称、颜色和新增子分类按钮直接可用。
- 子分类默认全部显示。取消勾选隐藏对应列；全部选中时按钮显示“取消全选”，部分或全部隐藏时显示“全选”。每个系列分别记住显示选择。
- 未分配到子分类的旧条目显示在“未分类”列。没有这些条目时不占空列；空系列提供默认列方便新增。
- 列标题随纵向滚动固定，多列支持横向滚动；每列独立加载，不再由前面的分类占满整个系列的加载额度。
- 每条收藏直接提供复制、编辑和展开。勾选、上下移和拖拽排序在“批量管理”中使用。
- 暂时移除收藏页内部搜索、系列查找、搜索筛选和旧折叠入口。主页全局 Tag 查询及 AI 查询仍然可以使用允许查询的收藏；从主页定位会切换到目标系列并显示目标子分类。
- 新增和切换系列前保存有效草稿；无效草稿留在编辑器中。新建条目及空白导入面板默认选择当前系列。

收藏存储格式没有变化，已导入的 `tag.xlsx` 内容无需重新导入。本次更新不改写用户数据目录，不添加依赖。

验证使用 `npm run check`（语法、21 条正确性规则、架构护栏、单元与 jsdom 测试、历史回归），以及独立临时数据的浏览器静态排版预览。最终测试数量、源码提交和打包校验记录在桌面目录 `build-info.json` 中。

人工验收建议：打开 `tag.xlsx 导入`，检查 9 个子分类横向分列；逐个取消勾选、取消全选后恢复，再新建一个系列切换查看。确认改名、配色、复制、缩放和关闭后重开符合预期。Electron 原生窗口、剪贴板和滚动手感仍需人工测试。

这是本地内部测试版，未推送，未发布 Release。
