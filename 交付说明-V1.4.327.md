# V1.4.327 · UI 统一优化测试版

基于 V1.4.326 优化现有工作台。桌面运行文件为 `AI绘画Tag工具箱V1.4.327.exe`。

## 设计参考

- [shadcn/ui Dashboard](https://ui.shadcn.com/examples/dashboard)：中性背景、轻边框卡片、统一控件与信息层级。
- [Linear Build](https://linear.app/build)：紧凑导航、克制强调色和清晰的工作区分隔。
- 图标来自 [Lucide 0.468.0](https://www.npmjs.com/package/lucide-static/v/0.468.0)，已内置 SVG 与对应许可证，不依赖在线加载。

## 这一版的变化

- 普通桌面窗口从三行导航收为两行，应用偏好放在顶部右侧，搜索和功能入口并列。
- 浅色使用浅灰底与蓝色强调，深色使用中性深灰底；统一按钮、输入框、圆角、间距、边框、悬停和键盘焦点。
- 分类图标、标签操作及主要导航统一为线性图标；标签卡片规则排列，操作按钮放在文字下方。
- 标签编辑弹窗采用双列辅助字段、清晰选项和固定底部操作区，验证错误与保存按钮同时可见。位置选择与未保存提示共用同一套外观。
- AI 初始页增加中英文欢迎说明，输入区作为完整组件呈现，API 设置采用统一卡片；修复选中页签悬停时的白字白底。
- 角色详情、翻译、收藏、识图和图片库复用同一套视觉变量；收藏与图片库在顶部显示当前工作区名称。

## 验证与边界

- `npm run check`：语法、lint、架构护栏、605 项测试及 `regressions-v194`。
- 使用实际 HTML、CSS 和视图脚本配合隔离的内存数据，核对标签、编辑弹窗、AI、API、角色、翻译、收藏和图片库页面。预览数据仅供视觉检查，不进入桌面包。
- 视觉检查覆盖浅色/深色与 1280 × 720、980 × 680 窗口；检查编辑器空内容校验、页签选中状态、页面导航与图标加载。
- 按项目规则跳过 `--uitest`、`--smoke`、`--i18ntest` 和启动应用的回归测试。本次没有验证真实 AI 请求、ComfyUI 出图或真实用户数据下的所有界面组合。
- 建议人工重点检查实际 Windows 缩放比例下的导航、长标签、编辑保存、收藏切换，以及 AI 会话和弹窗滚动。

内部测试版；不推送、不发布 Release。桌面包源码与 `app.asar` 校验记录见包根目录 `build-info.json`。
