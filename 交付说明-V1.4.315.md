# V1.4.315 单条收藏删除

在收藏页点击条目的铅笔按钮，编辑面板底部新增红色“删除收藏”。点击后会显示待删除条目的名称或 Tag，确认后从收藏页删除并关闭编辑面板。取消确认会保留当前输入。

删除使用现有收藏模块和页内确认框。即使正在编辑的 Tag 被清空，也可以删除已保存的条目。已有保存正在写盘时会先等待其结束；删除写盘失败会提示重试。尚未保存的新建草稿不显示删除按钮，可以使用关闭或放弃修改。

删除只移除该条收藏，收藏页、标签栏与其他条目保留。底部已选 Prompt 延续原有独立快照行为，仍可以通过底部的移除按钮单独移除。

验证：`npm run check`，包括取消删除、原文清空后删除、保存与删除并发、删除失败重试、重新加载和现有页面/标签栏删除回归。未启动带个人数据的 Electron 程序；按照项目规则，实际窗口点击与输入由用户人工验收。

本版为本地内部测试版，不推送、不发布 Release。
