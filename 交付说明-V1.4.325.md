# V1.4.325 · 继续缩短启动等待

V1.4.324 已修复分类/搜索全量传输，但统一词库仍在页面显示前读取并复制约42 MB基础数据，首页初始化又因语言设置重复渲染标签卡片。

本次调整：

- 私有JSON在完整校验后直接冻结使用，省去一份完整基础库复制；普通调用者仍使用独立副本。
- 完整Tag校验直接检查字段，不再为每条记录构造临时字段对象和重复字段集合；字段、类型、长度与引用规则不变。
- 分类计数与浏览不再提前构造全部搜索文本字段，首次搜索按需要创建并缓存。
- 保存语言先于首屏渲染应用，首页卡片只生成一次，去掉重复提示词面板渲染。

同机同一临时用户目录、188条模拟收藏、Electron31.7.7，窗口创建到页面就绪的测量：V1.4.320约810ms，V1.4.324约1572ms，本轮源码约955ms。首屏仍400条，数据条目未裁剪。测量不含双击启动进程前的系统调度、杀毒扫描和冷磁盘差异，不保证所有机器相同。

第一次搜索需要建立文本索引，本次实测高命中查询约97ms；后续使用缓存。原文复制、中文/分类、用户覆盖和迁移保存逻辑继续保留。

自动回归覆盖私有基础库原地冻结、无效数据仍被拒绝、默认副本隔离、计数不预读搜索字段、首页不重复渲染以及已保存语言首次显示/切换。最终检查见包外build-info.json。

测试均使用临时目录，没有读写真实收藏、图库或API配置。未push、未发布。
