# V1.4.339 · 多候选反馈目标保护

## 反馈路由

- 多张候选图存在时，普通文字反馈必须明确引用目标候选图或通过候选卡提交。
- 即使任务保存了默认选中候选，也不会把未指明目标的反馈静默改到该候选上。
- 歧义反馈会停在当前任务，提示用户选择候选，不调用主 AI、识图或 ComfyUI。

## 验证

- `npm run check`：660 项测试全部通过。
- 新增默认选中候选仍要求明确目标的回归测试。
- 未执行慢速 Electron/smoke 测试；真实模型和 ComfyUI 由用户人工验收。

运行 `AI绘画Tag工具箱V1.4.339.exe`。本轮未推送、未发布 Release。
