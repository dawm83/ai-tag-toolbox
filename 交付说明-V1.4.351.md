# V1.4.351 · 修复会话图片恢复

本版修复退出再打开后绘图图片不在会话区、候选显示为无法识别的问题。

原因是会话图片关系和 AI 消息结果分别保存；旧恢复逻辑只读取 `message.imageIds`，没有读取绘图结果里的候选和资源 ID。现在启动时会从消息、`message.artifacts`、绘图结果的 `imageIds`、`artifacts`、`candidates` 以及最终选择字段收集图片，并为当前会话补建关系。

图片本体仍由本地图片仓库持久化，关系按会话隔离。清空会话图片或删除会话时才会按引用计数删除没有其他引用的物理图片；普通退出不会删除图片。

另外修复了恢复后的对话图片卡片只读取轻量元数据、不读取本地预览文件的问题，并同步了 preload 桥接版本号。

检查：`npm run check`，719 项通过，0 失败；`git diff --check` 通过。真实模型、ComfyUI 和 Electron 启动仍交由用户人工验收。

本轮未 push、未发布 Release。
