# V1.4.346 · 修复图片直接粘贴

V1.4.346 修复了图片从系统剪贴板粘贴到 AI 对话框时的运行时错误，并保留 V1.4.345 的主 AI 可见评价和正向 Tag 复制行为。

## 本轮改动

- 根因是新粘贴图片没有元数据对象时，预加载层传入了 `null`，图片模块读取 `meta.displayName` 导致报错。
- 新图片的空元数据现在统一按空对象处理，粘贴后可以正常进入 AI 对话发送区。
- 剪贴板读取同时支持 `clipboardData.files` 和 `clipboardData.items.getAsFile()`，覆盖不同桌面应用复制图片时的 Electron 数据形态。

## 检查与使用

- `npm run check`：712 项通过，0 失败。
- 新增图片生命周期和 UI 回归测试，覆盖空元数据图片添加及仅有 clipboard item 的粘贴。
- 未执行付费 API、真实图片生成、真实 ComfyUI 或慢速 Electron 启动测试；这些仍需用户人工验收。
- 双击 `AI绘画Tag工具箱V1.4.346.exe`。本轮未 push、未发布 Release。
