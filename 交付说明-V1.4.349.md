# V1.4.349 · 修复迭代预算与角色 Tag 调度

V1.4.349 处理了两条绘图主流程问题：用户设置较高迭代次数时被主 AI 默认 8 回合提前截断，以及角色查询结果没有稳定进入首轮 Tag 编译。

## 本轮改动

- 绘图任务根据 `generation.maxAutoRounds` 动态扩充主 AI 的工具回合、工具调用和 ComfyUI 调用上限。设置 10 次迭代时，主 AI 可以完成每轮评价、Tag 修订、续绘和最终选图。
- 角色名任务建议先调用 `characters.search`。确认角色后，主 AI 可向 `generation.execute` 传 `characterIds` 和原始要求；编排器会解析角色身份 Tag，把它们与原始要求交给文生图 Tag 子代理，再使用子代理返回的 Tag 进行首轮绘图。
- 子代理输入仍保持精简：传用户原始要求和身份 Tag；角色库默认服装等未明确要求的外观不会自动混入。
- 保留普通文生图、参考图复刻、属性参考图和任务号自动绑定逻辑。

## 检查与使用

- `npm run check`：717 项通过，0 失败。
- 新增十轮迭代预算测试，以及角色查询到 Tag 子代理的真实 Assistant 链路测试。
- 未执行付费 API、真实图片生成、真实 ComfyUI 或慢速 Electron 启动测试；这些仍需用户人工验收。
- 双击 `AI绘画Tag工具箱V1.4.349.exe`。本轮未 push、未发布 Release。
