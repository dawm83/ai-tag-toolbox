# V1.4.330 · 收藏星标与搜索框

收藏后显示金色实心五角星，移开鼠标仍然可见。再次点星星会确认是否取消收藏；取消确认不修改数据，保存失败时仍保留已收藏状态。

- 标签主页：确认框列出受影响的收藏位置，确定后取消这些位置的收藏关系，保留标签原文、中文信息、角色引用和当前选择。
- 快捷收藏：每条记录的取消入口改为点亮星星，确认后只移除当前收藏组内的那条关系，其他组中的同一标签不受影响。
- 收藏位置菜单保留跳转功能，并提供“收藏到其他位置”。角色详情的身份收藏同步星标状态；所选特征组合仍沿用原有组合收藏流程。
- 搜索框在普通和最大化窗口中最大宽度为 560 像素，小窗口可继续收缩。

## 检查

- `npm run check`：610 项测试全部通过，含新增的收藏确认/取消、保存失败、多个收藏位置、撤销、角色身份星标及重载持久性验证。
- 隔离页面预览核对常亮实心星、收藏列表星形按钮、取消确认框，以及普通/最大化窗口的搜索框宽度。
- 按项目规则未启动 Electron 慢测试；真实 Windows 缩放、用户数据与完整桌面交互仍待人工验收。

运行 `AI绘画Tag工具箱V1.4.330.exe`。本版未推送、未发布 Release，打包核验见包根目录 `build-info.json`。
