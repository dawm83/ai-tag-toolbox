# V1.4.322 验证记录

## 范围

接手基线 fcffc29。统一标签 Task 10/11 收尾、标准搜索空格匹配、识图中文分类，以及内置显示名称/分类补全。代码沿用 Electron、CommonJS、原生 DOM/CSS 与现有 JSON 持久化，无新运行依赖。

## 已完成证据

- 接手基线：`npm run check` 560 项通过，日志 `F:/codex/AI绘画Tag工具箱/work/unified-takeover-baseline.log`。
- 整合检查：591 项通过，日志 `F:/codex/AI绘画Tag工具箱/work/unified-enrichment-check.log`。新增完整旧内置库升级用例后，V1.4.322 最终检查 592/592 通过，日志 `F:/codex/AI绘画Tag工具箱/work/unified-v14322-final-check.log`。
- 迁移/导入/批量复核发现两项问题并修复：收藏页筛选范围刷新；撤销删除标签后清除悬空最近记录。原 Prompt 引用保护不变。
- 元数据升级复核修复一项问题：用户只覆盖子分类时保留其旧父分类。使用真实临时文件验证旧文件备份、失败重试、备份恢复和第二次启动不重复保存。
- 真实 53,707 条旧随包基础库的内存升级验证通过：保持标签总数、ID/英文原文、收藏引用和用户明确空中文；新默认符华装甲译名可见。
- `node scripts/build-unified-tags.mjs --check` 校验生成产物可重现，兼容清单的源指纹与 fcffc29 随包库一致。
- 识图 DOM 测试验证中文、分类、概率、文本安全呈现、单项/整组原文复制与编辑后刷新。收藏定位可打开关闭页和隐藏组并定位第 125 个条目。
- 未读取或修改真实用户 AppData；所有写入验证为内存库或临时文件。

## 性能

Windows x64，Node v24.19.0，Ryzen 7 9800X3D；同机固定完整 53,707 词库，构造 10,000 条收藏。日志 `F:/codex/AI绘画Tag工具箱/work/unified-final-performance.json`。

| 测量 | 当前 |
| --- | ---: |
| 库实例初始化，不含读取基础 JSON | 1,222 ms |
| 导入预览及提交 10,000 收藏 | 1,679 ms |
| 首次收藏搜索，包含共享索引首次建立 | 184 ms |
| 缓存搜索 p50 / p95 | 0.72 / 1.06 ms |
| 编辑并重新建立索引 | 355 ms |
| 全收藏布局数据读取 | 51 ms |

原 P0 基线为 19,692 标签及 10,000 收藏，首次收藏搜索 80 ms、缓存 p50/p95 6.18/8.74 ms、编辑 108 ms。当前基础库增至约 2.7 倍且共享多域引用，因此冷启动与编辑不是等规模对比；缓存搜索改善明显。P2 同等 53,707 词库/10,000 收藏记录初始化 1,488 ms、主页冷索引 213 ms、收藏缓存约 1 ms，与本次量级一致。未将进程启动、模型加载或真实磁盘耗时混入内存数字。

## 待人工验收

Electron 真实窗口、系统剪贴板、IME、拖动、下载文件和窗口关闭。真实 AI、绘世 ComfyUI 和本地模型未调用。646 条未译专属词及冷门人名需继续核实来源，不能将本轮补全视为全库中文覆盖。
