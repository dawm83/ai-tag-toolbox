# V1.4.324 · 启动与搜索卡顿优化

本次集中修复 V1.4.323 打开慢、分类和搜索操作卡顿。

## 原因与调整

- 53,707 条基础词库在启动时多次完整复制和校验；首次迁移还会再次准备同一基础库。现在构造一次经过完整校验的不可变快照，生产入口、迁移和引用验证共用它。
- 原先修改查询或分类会生成并通过 Electron bridge 返回整组数据，然后页面又查询前400条。常见查询可命中4万多条，虽然界面只显示400条，仍会阻塞传输。现在设置接口只返回新状态，由分页查询读取实际需要显示的结果。
- 外部可编辑基础库调用仍获得独立副本，用户数据覆盖与基础库分离。无效数据、引用校验、保存失败、迁移备份与 V1.4.323 的原子保存修复继续保留。

## 同机测量

完整 Electron 31.7.7 preload 和页面，临时用户目录、188 条模拟收藏、同一已迁移 v2 数据；未读取真实用户收藏。单次对比数字用于说明量级，不是跨机器保证。

| 操作 | V1.4.323 | 修复后 |
| --- | ---: | ---: |
| 窗口创建到页面就绪 | 2,823 ms | 1,630 ms |
| 切换服装分类 | 90 ms | 20 ms |
| 搜索 a（包含结果处理与DOM更新） | 1,546 ms | 50 ms |
| 清空查询 | 48 ms | 12 ms |

页面仍显示400条首屏结果，数量与分页规则未缩减。高命中搜索测试验证的是实际按钮事件的同步处理耗时；GPU绘制和真实大规模用户资料仍受设备与数据影响。

新增回归保护：设置接口不查询全量、只有分页接口读取结果；已验证快照不可修改；不同用户库共享基础数据时编辑互不影响；默认加载副本可独立修改。完整检查结果见 `build-info.json`。

不改动用户文件，旧 V1.4.323 安装目录仅作本地备份，未推送或发布 GitHub。
