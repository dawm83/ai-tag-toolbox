# V1.4.352 · 调整候选图智能选择流程

本版调整图片修改入口。用户明确说“图 20”“第 20 张”“上一张”“最后一张”或候选图片 ID 时，系统直接把修改要求绑定到当前会话对应候选并继续续绘；不会再要求用户回到图片下方重新输入。

当文字无法唯一确定目标时，主 AI 会结合当前会话的候选图、槽位、轮次、选中状态和已有评价作判断。主 AI 仍无法判断时，才显示候选选择卡；用户点击候选后会沿用原修改意见继续任务。选择结果会校验任务属于当前会话、候选仍存在且图片已挂载，避免跨会话或失效图片被续绘。

本轮新增 `generation.resolveTarget` 工具、候选解析模块和 UI 选择卡，并覆盖明确图号、主 AI 选择、主 AI 询问以及非法目标保护路径。

检查：`npm run check`，729 项通过，0 失败；`git diff --check` 通过。真实模型、ComfyUI 和 Electron 启动仍交由用户人工验收。

本轮未 push、未发布 Release。
