# V1.4.322 · 统一标签与中文分类

本版本完成统一标签、收藏与角色特征改造，并保留 V1.4.320 的 ComfyUI 连接、仅生成 Tag 和断线恢复改动。

## 使用变化

- 主页、收藏和角色特征共享同一份标签，使用同一个编辑弹窗。修改名称、原文、别名、备注或分类会同步到所有引用位置；取消不保存。
- 收藏同一标签到多个分组只新增位置。取消收藏不删除标签；“另存为独立标签”才创建互不影响的副本。
- 主页显示备注、搜索关闭状态及收藏位置入口。旧选择内容以“旧快照”标识，原文保留。
- 关闭“允许搜索”会同时影响主页、收藏内部及 AI 发现式搜索，直接浏览位置仍可用。
- 收藏页支持导入预览、全库/仅收藏备份、迁移报告和跨页批量整理。大备份自动使用 JSON gzip，导入前可取消。
- 标准搜索支持 `fuhua`、`fu hua`、`fu_hua`，精确搜索仍保持原规则。查找结果使用现有名称和别名，字面精确命中优先。
- 识图结果显示中英文、细分类和模型概率；复制单词或整组仍保留原英文和原顺序。
- 补充 2,002 个显示名，修正 1,518 个分类位置，包含 438 个同作品衣装组合名与 158 个角色专属词。已有中文及实际 Tag 内容不改写。

## 首次打开与数据

标签、收藏、角色编辑数据仍保存在用户 AppData 目录，安装包不包含个人收藏、图库、API Key 或对话。首次打开会备份旧标签相关数据并迁移到 `tag-library-v2.json`；已有 v2 则读取同一份用户覆盖。

本次内置中文更新只替换经检查的名称与分类默认值。用户显式改名、清空名称及分类覆盖保留；升级保存失败时原文件不变，并提供重试。既有引用、历史对话、已发送 Prompt 及复杂组合原文不会被新中文改写。

迁移前请关闭仍在运行的旧版。新旧版本的编辑不会自动相互同步；若要回到旧版，应先导出新版中需要保留的修改，旧版只能看到旧存储。

## 验证与剩余范围

最终自动检查结果见 `docs/qa/unified-tag-library/verification-v14322.md`，包内 `build-info.json` 记录源码提交、asar 和源文件校验。

模型通用词的空白中文已补齐。仍有 646 个角色专属词缺中文，部分冷门人物、作品与具名皮肤继续保留英文；未用未经核实的猜译冒充官方名。衣装组合名为描述性中文，人工词表未宣称全部经过官方审定。

按项目规则未启动真实 Electron 慢测，也未调用真实 AI 或绘图。原生窗口、剪贴板、输入法、拖动和实际模型需用户按 `docs/qa/unified-tag-library/manual-acceptance.md` 验收。

未推送 GitHub，未发布 Release。
