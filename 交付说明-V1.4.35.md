# V1.4.35 收藏页工作簿式交互

本版按工作簿方式重做收藏页。

- 第一排是收藏页页签。每个页签代表一整套独立收藏内容，点击后整体切换。右侧加号新建收藏页；页签可拖动排序，右键可以重命名、改颜色或删除，删除需要确认。
- 第二排是当前收藏页的子标签栏页签。默认创建一个“新建标签栏”，右侧加号在当前收藏页新增一列；列可以重命名、改颜色、删除和拖动交换顺序。
- 下方只显示当前收藏页的所有列。每列顶部与第二排标题同步，下面纵向排列现有 Tag，并始终保留一个“点击新增标签”的空白卡。
- 点击空白卡只打开轻量录入：Tag 原文、名称/中文、可选备注。保存后空白卡继续保留，便于连续录入。
- 已有 Tag 继续使用主页风格的复制、选择、编辑和展开操作。备注通过鼠标悬停在卡片右侧显示。
- 暂不恢复收藏页内部搜索；主页全局查询仍按原来的可发现性规则工作。

数据域仍使用现有 series、sections、entries，V1.4.34 与 tag.xlsx 导入内容可直接继续使用。旧版本缺少子标签颜色时会按默认调色板补齐，不改变 Tag 原文。

验证：npm run check、工作簿交互 jsdom 测试和静态布局预览。Electron 原生窗口、鼠标右键、拖动手感和系统剪贴板仍需人工验收。本版未推送、未发布。
